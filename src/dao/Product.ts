export interface Product {
  id: string;          // unique identifier as string
  name: string;        // product name
  img: string;         // image path
  categories: string;  // space-separated categories
}